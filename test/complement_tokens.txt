# Sample token definitions

# Unknown tokens should have lowest priority, so they are declared first
#
UNKNOWN: [\u0000-\uffff]

WS:   ( [\f\r\s\t\n]+ ) \
    | ( \# [^\n]* \n? )


NOTZULU: ^(ZULU)

A: A

